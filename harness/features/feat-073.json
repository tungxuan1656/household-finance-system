{
  "id": "feat-073",
  "name": "Yellow Finance protected pages refactor plan and canonical page rules",
  "status": "in_progress",
  "description": "Plan the frontend-only refactor of the protected `apps/web` experience around the approved Yellow Finance mobile-first shell and screens, and update the canonical frontend docs so future page work follows the new shared page-wrapper rules instead of `PageShell`.",
  "dependencies": ["feat-067", "feat-072"],
  "plan": "docs/exec-plans/plans/2026-05-27-yellow-finance-protected-pages-refactor.md",
  "evidence": {
    "planned": [
      "Used using-skills first, classified the work as Level 2, and used writing-plans because the user explicitly asked for an implementation plan before the multi-batch frontend refactor.",
      "Read docs/PLANS.md, docs/exec-plans/__plan-template__.md, docs/exec-plans/index.md, docs/design-docs/mobile-first-protected-shell-and-tab-surfaces.md, docs/FRONTEND.md, and the key frontend reference docs before drafting the ExecPlan.",
      "Mapped the current protected route pages, shell/layout files, auth redirect touchpoints, route constants, and current PageShell dependency points so the plan names exact files instead of generic frontend areas.",
      "Updated canonical frontend docs so future page work now points to the new protected page-surface wrapper pattern and the four-tab responsive shell model instead of creating new PageShell-based pages.",
      "Started implementation with the shared wrapper foundation batch: added `apps/web/src/components/shared/page/` and defined `PageContainer`, `PageHeader`, `PageContent`, `PageFooter`, plus the shared barrel export.",
      "Continued implementation with the first navigation batch: moved the protected default entry to Expense semantics, turned `/home` into a compatibility redirect, reduced shared mobile/desktop navigation to the four approved top-level tabs, and restyled the mobile bottom tab bar closer to the approved Yellow Finance surface.",
      "Started the Expense surface migration: moved `ExpensesPage` onto the new shared page wrappers, grouped the feed into timeline sections, and added context badges for household/group-linked expenses while preserving the current hook-based data flow and global add-expense entry point.",
      "Rebuilt the add-expense entry into a shared three-step bottom drawer flow on top of the existing create hook and submit logic: category/date selection, amount/source/note entry with quick presets, and final household/group assignment.",
      "Migrated the Analysis page to the new shared page wrappers, replaced the period select with chip-style period buttons, and reordered the data sections so the donut/category allocation card leads on mobile before the comparison and grouped-breakdown panels."
    ],
    "verification": [
      "Validated harness/feature_index.json and harness/features/feat-073.json with python3 -m json.tool.",
      "Checked the new ExecPlan and updated frontend docs for the expected Yellow Finance protected-shell and page-wrapper terms.",
      "Planning/docs session verification passed earlier with final ./init.sh returning Done! and gitnexus_detect_changes(scope: all) recording the docs-only blast radius.",
      "Pre-edit GitNexus impact checks for `PageShell` and `MobileHeader` both returned LOW risk with 0 direct dependents and 0 affected processes before the first shared-wrapper code batch.",
      "Shared wrapper foundation verification passed: ./init.sh typecheck returned OK and ./init.sh lint returned OK.",
      "Early gitnexus_detect_changes(scope: all) during the implementation session returned no changed symbols because the new shared wrapper files were still untracked.",
      "Pre-edit GitNexus impact checks for `BottomTab`, `AppSidebar`, and `HomeRoutePage` all returned LOW risk with 0 direct dependents and 0 affected processes before the navigation batch.",
      "Navigation batch verification passed: ./init.sh typecheck returned OK and ./init.sh lint returned OK.",
      "Post-batch gitnexus_detect_changes(scope: all) returned LOW risk with 10 changed symbols across 7 files and 0 affected processes.",
      "Pre-edit GitNexus impact checks for `ExpensesPage` and `AddExpenseDialogProvider` both returned LOW risk with 0 direct dependents and 0 affected processes before the Expense batch.",
      "Expense batch verification passed: `pnpm --filter web exec vitest run src/features/expenses/pages/expense-feed-page-helpers.test.ts` passed with 5 tests; `./init.sh typecheck` returned OK; `./init.sh lint` returned OK.",
      "Post-Expense gitnexus_detect_changes(scope: all) returned LOW risk with 5 changed symbols across 5 files and 0 affected processes.",
      "Add-expense flow helper verification passed: `pnpm --filter web exec vitest run src/features/expenses/components/add-expense-flow-helpers.test.ts src/features/expenses/components/use-expense-entry-form.test.ts` passed with 13 tests; `./init.sh typecheck` returned OK; `./init.sh lint` returned OK.",
      "Post-add-expense gitnexus_detect_changes(scope: all) returned LOW risk with 5 changed symbols across 2 indexed files and 0 affected processes.",
      "Analysis batch verification passed with sandbox-safe alternatives: `apps/web/node_modules/.bin/tsc --noEmit --project apps/web/tsconfig.json` returned no output/success, and `/Users/tungdoan/.nvm/versions/node/v24.15.0/bin/pnpm --filter web lint` completed without lint errors after the layout/header/chart reorder.",
      "Repo-standard `./init.sh typecheck` could not be used in the current sandbox because `pnpm` was unavailable in PATH there, so the focused TypeScript check above was used as evidence instead.",
      "Household batch verification passed: `./init.sh typecheck` returned OK; `./init.sh lint` returned OK; `./init.sh test` returned OK. HouseholdsPage and HouseholdDetailPage both migrated from PageShell to PageContainer/PageHeader/PageContent with showBack on detail page.",
      "Settings batch verification passed: `./init.sh typecheck` returned OK; `./init.sh lint` returned OK; `./init.sh test` returned OK. SettingsPage migrated from PageShell to PageContainer/PageHeader/PageContent, preserving DataState customAction retry behavior."
    ]
  },
  "notes": "Execution has started. All four top-level tab pages (Expense, Analysis, Household, Settings) are now migrated to shared wrappers. Browser-based visual verification and Batch 7 cleanup (PageShell deletion, docs finalization) are still pending."
}
