# Session: 2026-06-19 — TMA AI expense import end-to-end

## Scope

feat-109: Worker OpenAI-compatible AI parse endpoint + TMA text-to-preview expense import flow.

## Done

- Plan: `docs/exec-plans/plans/2026-06-19-tma-ai-expense-import.md`
- Worker:
  - OpenAI-compatible chat completions client (`apps/worker/src/lib/ai/expense-parser.ts`) using env `OPENAI_COMPAT_BASE_URL/KEY/MODEL`, 20s AbortController timeout, JSON-only `response_format`, no raw-text logging, defensive parse (empty array on failure).
  - Strict Zod parse contracts (`apps/worker/src/contracts/expense-parse-schemas.ts`) with `parsedExpenseItemSchema` refined for `expense` category kind via `categoryKindMap`.
  - Protected handler (`apps/worker/src/handlers/expenses/parse-expense.ts`) — applies defaults (`sourceKey=bank-transfer`, `occurredAt=defaultOccurredAt`), validates via `safeParse`, drops invalid items, regex-checks AI `occurredAt` format, never writes D1.
  - Route `POST /api/v1/expenses/parse` registered in `apps/worker/src/routes/expenses.ts` before `:id`, with explicit `use('/expenses/parse', authMiddleware)`.
  - Env types + `.dev.vars.example` updated.
  - Integration tests (`apps/worker/test/integration/expenses-parse.spec.ts`): 5 cases with `vi.mock` for the AI parser.
- TMA:
  - `import-api.ts` (`parseExpenses` + `useParseExpensesMutation`).
  - `import-store.ts` (Zustand `useImportFlowStore` — items[]/setItems/toggleInclude/setItemContext/setItemStatus/reset).
  - `import-confirm.ts` (sequential `createExpense` with no-dup retry on success status, partial-failure result).
  - `add-expense-chat.tsx` (text entry + BottomButton "Phân tích"; reset on entry from expenses list, not on mount, to preserve rawText on back-nav).
  - `add-expense-import-preview.tsx` (per-item include checkbox, household+group NativePicker, animated cards via `--animate-tma-card-enter` with `prefers-reduced-motion`).
  - AI button in `expenses.tsx` header.
  - Routes `expensesNewChat`/`expensesNewImport` + lazy route entries.
  - 15 i18n keys in `vi.json` (`aiImport`, `includeItem`, `importSuccess` polished).
  - Tokens `--color-tma-error`, `--color-tma-error-bg` added to `index.css` and consumed by chat/preview.
  - 21 import tests + 1 new retry-skip test (97 tests total).

## Reviews

- Oracle Worker review: 4 real issues found (parsedExpenseItemSchema not runtime-validated; route auth not explicit; AI occurredAt not validated; 30s timeout too close to wall) + 1 acceptable MVP gap (no rate limit). All 4 fixed; gap logged.
- Designer TMA review: 4 priority issues (mount reset wiped rawText on back-nav; selectedCount/confirm didn't skip status=success on retry; hardcoded #d93838/#ffeded; no motion). All 4 fixed.

## Verification

- `./init.sh typecheck` -> OK
- `./init.sh test` -> OK (Worker 433/82, TMA 97/20)
- `./init.sh` -> Done! (after removing one stale `rawText` selector from preview that lint flagged as `unused-imports/no-unused-vars`)

## Decision log

- Two-endpoint split (parse + create) preserved; AI output never reaches D1 write path.
- Defaults: `sourceKey=bank-transfer`, `occurredAt=client-local today`.
- Sequential confirm through existing `POST /expenses`; partial success allowed, no rollback.
- Include/exclude per item, no inline parsed-field editing. Back-nav re-enters text.
- TMA local date chosen over Worker UTC to avoid Vietnam-tz drift.
- Rate limit, multi-provider UI, caching, web parity: explicit out-of-scope.

## Out of scope / deferred

- Per-user AI rate limit middleware (Oracle acceptable MVP gap).
- Web parity.
- Editing parsed fields inside preview.
- Feedback loop / parse cache / history persistence.

## Commit

None. User did not request.
