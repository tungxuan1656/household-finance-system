# Frontend References Index

Frontend implementation rules split by surface.

## Surfaces

- `web/`: `apps/web` implementation rules. Start at `web/index.md`.
- `tma/`: `apps/tma` implementation rules. Start at `tma/index.md`.

## Rule

- Do not point TMA work at web implementation leaves unless the rule is truly shared and has been lifted into a new shared home.
